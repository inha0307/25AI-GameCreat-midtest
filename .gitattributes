# 자동으로 텍스트 파일 줄바꿈을 맞춰줌
* text=auto

# C# 및 Unity 관련 파일은 Windows 스타일(CRLF)
*.cs text eol=crlf
*.shader text eol=crlf
*.meta text eol=crlf
*.asset text eol=crlf
*.unity text eol=crlf

# 쉘 스크립트나 리눅스용 파일은 LF로
*.sh text eol=lf
